import 'package:flutter/material.dart';

class ProductLongCard extends StatelessWidget {
  const ProductLongCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
